# Full Skill Archive (2026-03-11)

This archive preserves the broader structure and rationale behind the Google Docs connector skill before it was over-trimmed.

## Archived Guidance Summary

- connector-first editing quickstart
- browser attachment and runtime guidance
- request-shape reminders and tab-aware calls
- prompt, bullets, links, and heading rules
- native Google Docs table workflow
- appendix and table styling guidance
- minimal execution loop and reference-routing rules

## Why The Prior Trim Failed

The previous trim removed too many normative rules from `SKILL.md` and replaced the references with placeholder summaries instead of usable extended guidance. That created two problems:

1. the main file no longer strongly enforced the successful brief-formatting behavior
2. the references no longer contained enough detail to recover the missing behavior when needed

Use the repaired `SKILL.md` as the enforcement layer and these references as actual supporting documents, not placeholders.
