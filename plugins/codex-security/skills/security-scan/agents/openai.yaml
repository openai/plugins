interface:
  display_name: "Security Scan"
  short_description: "Run security scan"
  default_prompt: "/goal Run a security scan on this codebase using subagents. Do not stop until all in-scope files are covered and all required steps are complete."
